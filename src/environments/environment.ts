// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  //  rootURL : "https://events.chinmayamissionthane.in/api/v1" //Prod
// rootURL: "https://e263-1-186-177-201.ngrok.io"
  // rootURL = "http://localhost:3000"; //localhost
  //rootURL = "http://192.168.1.37:8000";
  rootURL: "https://stg.events.chinmayamissionthane.in/stg-api/v1" //Stg
// 
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
